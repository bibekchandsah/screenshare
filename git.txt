
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/bibekchandsah/screenshare.git
git push -u origin main

git remote add origin https://github.com/bibekchandsah/screenshare.git
git branch -M main
git push -u origin main




1) secure login with one or multiple user with security code
2) server confirmation before client access
3) confirmation before closing screen preview
4) in same wifi user can share screen in mobile devices too
5) check the server connection and disconnection so that user can see when it's dissconnected or connected
6) user should be able to see the cursor too
7) user from anywhere can see the shared screen

